# Handoff de Plataforma y Documentación — 2026-08-23

Documento autocontenido para retomar el trabajo del SaaS FinOps desde cualquier entorno o sesión. **No hace falta leer la conversación previa.**

- **Rama:** `main` (alineada con `staging` tras la ejecución exitosa de CI y Deploy)
- **Últimos Commits:**
  - `c9e177a` — `docs: synchronize LLD, HLD, user/superadmin manuals (ES, EN, PT-BR), and compiled PDFs`
  - `d9ec769` — `fix(db): unify SaaSCronJobs schema and seed with storage retention cleaner`
  - `bd332ed` — `fix(migrations): use standard MySQL 8.0 ALTER TABLE ADD COLUMN syntax`
  - `3648a2f` — `fix(i18n): add commercialUpdated key to es, en, pt-BR and update cron count in operations test`
  - `56dad4d` — `fix(notifications): avoid mutating activeTenantRef during render in useTenantNotifications`
  - `961fc86` — `feat(db): generate consolidated MySQL 8.0 DDL platform schema migration and seed catalogs`
- **Producción:** 100% Operativa y Desplegada en Azure Container Apps (`westus2`) — Health check `https://finops.cscloudsolutions.com.ar/api/health` → HTTP 200.

---

## 1. Resumen de Implementaciones y Features Entregados

### A. 🗄️ Esquema Consolidado DDL MySQL 8.0 y Catálogos de Semillas
- **Ubicación:** [`migrations/20260823-003-consolidated-platform-schema.sql`](../migrations/20260823-003-consolidated-platform-schema.sql)
- **Alcance:** Soporte completo e idempotente para los 6 dominios de la plataforma SaaS FinOps:
  1. *Core & Tenants:* `Tenants`, `TenantSubscriptions` (cuotas por tier), `TenantCommercialDeals`, `TenantGlobalSettings`.
  2. *Configuración e Integraciones:* `TenantIntegrations` (ITSM Jira/Azure DevOps/ServiceNow), `TenantAiSettings` (Azure OpenAI / Modelos), `NotificationChannels` (Slack/Teams/Email/Webhook), `TenantM365CopilotSettings`, `TenantMcpApiKeys`, `TenantPublicApiKeys`.
  3. *Reportes y Facturación:* `ExecutiveReportJobs` (jobs asíncronos), `ExecutiveReportHistory` (retención de blobs PDF/JSON), `TenantMarkupSettings` (CSP margins), `MarkupOverrideRules`, `TenantFocusSchedule`.
  4. *Operaciones y SuperAdmin:* `Notifications`, `TenantNotifications`, `AuditTrailLogs` (inmutable), `SaaSCronJobs` (estado y duraciones), `SaaSComponentHealth` (MySQL, Redis, ARM, Blob, OpenAI, SMTP), `TenantPartnerCenterAssociations` (PAL / CPOR), `BillingPricingUnitsCatalog` y `PricingUnits` (45 unidades FinOps Toolkit).
  5. *Semillas Iniciales:* Catálogo de 9 cron jobs, 6 componentes core, configuración maestra de IA y 45 PricingUnits.

### B. 🧹 Motor Desatendido de Purga y Retención por Tier (`storage-retention-cleanup`)
- **Servicio y API Route:** [`src/services/storageRetentionCleaner.service.ts`](../src/services/storageRetentionCleaner.service.ts) y [`src/app/api/cron/storage-retention-cleanup/route.ts`](../src/app/api/cron/storage-retention-cleanup/route.ts).
- **Políticas de Retención Contractual:**
  - **Tier Professional:** 90 días de retención histórica de reportes PDF/JSON.
  - **Tier Business:** 180 días de retención histórica de reportes PDF/JSON.
  - **Tier Enterprise:** 365 días de retención histórica de reportes PDF/JSON.
- **Flujo:** Búsqueda de candidatos expirados, purga física de blobs en Azure Blob Storage (`reports-pdf`, `reports-json`), borrado suave en DB (`deleted_at = NOW()`), registro en `AuditTrailLogs` y actualización de métricas en `SaaSCronJobs`.

### C. 🛡️ Motor de Impersonación de Sesión SuperAdmin (God Mode Delegado)
- **Servicio y Componente:** [`src/services/sessionImpersonation.service.ts`](../src/services/sessionImpersonation.service.ts) y [`src/components/superadmin/ImpersonationBanner.tsx`](../src/components/superadmin/ImpersonationBanner.tsx).
- **Seguridad:** Cookie HTTP-only cifrada `saas_impersonation_session` (validez 4 horas), banner flotante en capa `z-[90]` con botón de salida instantánea, y registro inmutable en `AuditTrailLogs` con `isImpersonated: true`.

### D. 🔔 Centro Global de Notificaciones y Enforzamiento de Cuotas
- **Componentes:** [`src/components/layout/NotificationBellDropdown.tsx`](../src/components/layout/NotificationBellDropdown.tsx), [`src/components/subscription/UpgradeModal.tsx`](../src/components/subscription/UpgradeModal.tsx), [`src/hooks/useTenantPlanLimits.ts`](../src/hooks/useTenantPlanLimits.ts).
- **Navbar:** Campana interactiva con badge azul institucional (`#0078D4`), polling background y filtros dinámicos (Todas, No Leídas, Info, Advertencias, Críticas).
- **Límites de Suscripciones:** Professional (máx. 2), Business (máx. 3), Enterprise (ilimitado) con modal de upgrade integrado a Paddle B2B.

---

## 2. Documentación Sincronizada y Compilada (`/actualiza-docu`)

| Archivo Documental | Formato | Estado |
| :--- | :---: | :---: |
| [`docs/lld/00-lld-completo.md`](lld/00-lld-completo.md) | Markdown | Actualizado (Tablas, Crons, Cuotas, Impersonación) |
| [`docs/lld/LLD-FinOps-CSCloudSolutions.pdf`](lld/LLD-FinOps-CSCloudSolutions.pdf) | PDF (5.5 MB) | Compilado con Playwright y diagramas Mermaid SVG |
| [`docs/hld/00-hld-completo.md`](hld/00-hld-completo.md) | Markdown | Actualizado (Matriz de Tiers y Retención de Blobs) |
| [`MANUAL_DE_USUARIO.md`](../MANUAL_DE_USUARIO.md) | Markdown | Actualizado (§3.8 Notificaciones & Cuotas, §3.9 Tiers) |
| [`docs/manual/MANUAL_USUARIO_{ES,EN,PT-BR}.md`](manual/) | Markdown | Actualizado en los 3 idiomas (§8.4.1 y §8.5.1) |
| [`docs/manual/MANUAL_SUPERADMIN_{ES,EN,PT-BR}.md`](manual/) | Markdown | Actualizado en los 3 idiomas (Paso 5 Impersonación) |
| [`docs/manual/MANUAL_{USUARIO,SUPERADMIN}_{ES,EN,PT-BR}.pdf`](manual/) | 6 PDFs | Compilados y sincronizados en `docs/manual/` y `public/manual/` |

---

## 3. Estado de CI/CD y Calidad

- **Typecheck:** `tsc --noEmit` → **0 errores**.
- **Linting:** `eslint --quiet` → **0 errores** (3025 warnings tolerados).
- **Unit & Integration Tests:** 1706 tests passing en Vitest (`100% passing`).
- **GitHub Actions CI (Staging):** Run `32648821645` → **SUCCESS** ✅.
- **GitHub Actions Deploy (Main):** Run `32648826256` → **SUCCESS** ✅ (Container App Job de Migraciones OK, Deploy Runtime OK, Deploy 14 Cron Jobs OK, Health Check HTTP 200 OK).

---

## 4. Estado de Git y Pasos para Próxima Sesión

```bash
# Estado de git:
# Rama main está 1 commit por delante de origin/main (el commit c9e177a de documentación).
# Para sincronizar remotamente cuando el usuario lo solicite:
git push origin main:staging
git push origin main:main
```
