# Auditoría de Seguridad — 2026-08-09

**Tipo:** Auditoría incremental de remediación.
**Alcance:** rutas de diagnósticos y métricas de bases de datos, AI Cost Analytics y cron `sync`.
**Metodología:** revisión estática de rutas y dependencias, más pruebas Vitest dirigidas de autorización, precisión monetaria y cancelación.

## Estado de findings

| Finding | Severidad | Estado | Remediación verificada |
|---|---|---|---|
| SEC-01 | HIGH | ✅ REMEDIADO | `requireTenantAccess` se ejecuta antes de los branches `isMockTenant` en rutas afectadas. |
| FIN-01 | HIGH | ✅ REMEDIADO | Agregaciones y distribuciones de costo usan `decimal.js`; el redondeo a centavos es explícito. |
| OPS-01 | MEDIUM | ✅ REMEDIADO | Ausencia de telemetría se informa como no disponible, sin historiales cero fabricados. |
| REL-01 | MEDIUM | ✅ REMEDIADO | El deadline del cron aborta trabajo cooperativo y bloquea escrituras tardías. |

## Verificación

- `__tests__/integration/mockDiagnosticsAuthorization.test.ts`: confirma que las rutas de diagnósticos MySQL y Redis devuelven `403` ante un tenant demo no autorizado.
- `__tests__/unit/diagnosticsSharedMoney.test.ts`: confirma distribución Decimal y redondeo determinista a centavos.
- `__tests__/unit/cronSyncDeadline.test.ts`: confirma propagación de aborto, éxito dentro de plazo, errores tempranos y control de rechazos tardíos.
- `__tests__/unit/billingServiceScope.test.ts`: confirma el alcance de facturación después de propagar señales de cancelación.

## Riesgo residual

La exactitud de cada muestra depende de la disponibilidad y agregación expuestas por Azure Monitor. Las muestras ausentes se preservan como `null`; el panel no las presenta como actividad o capacidad cero.