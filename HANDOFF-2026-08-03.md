# Handoff — sesión 2026-08-03

## Estado final

- Cambios integrados en `main` y desplegados en Azure con éxito.
- Último commit en producción: `2d580ca`.
- Workflow de deploy completado en verde:
  - `https://github.com/manny864/finops/actions/runs/30829805208`

## Cambios implementados (tramo actual)

### 1) Ops SuperAdmin: crons por entorno

Se ajustó el backend para que en **producción** solo se muestren crons de **Azure** (catálogo oficial), y en entorno no productivo se mantenga la unión con crons observados/locales.

- Archivo:
  - `src/app/api/superadmin/ops/route.ts`

### 2) Comercial por tenant + robustez API

Se consolidaron cambios previos de comercial (vendedor/comisión) y hardening para evitar errores por diferencias de esquema entre entornos.

- Archivos relevantes:
  - `src/app/[locale]/admin/tenants/page.tsx`
  - `src/app/api/admin/tenants/route.ts`
  - `src/app/api/tenants/route.ts`
  - `migrations/20260803-002-tenants-sales-commission.sql`

### 3) Documentación sincronizada

Se actualizaron README, HLD/LLD, manuales ES/EN/PT-BR y PDFs asociados (usuario y superadmin), incluyendo artefactos regenerados en `docs/lld/generated/*` y `public/manual/*`.

## Historial de integración

- Commit local: `01954ef` (antes de rebase)
- Push rechazado por fast-forward.
- Rebase sobre `origin/main`.
- Commit resultante en remoto tras rebase: `2d580ca` (en `origin/main`).

## Punto de control operativo

- Requisito pedido por usuario cumplido:
  - **“lleva a producción solo los cron de azure y los locales deben quedar en local”**.
